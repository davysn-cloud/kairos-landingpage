# Server port (default: 5000)
PORT=5000

# PostgreSQL connection string (required for database features)
# DATABASE_URL=postgresql://user:password@localhost:5432/kairos

# Site URL for OpenGraph meta tags (optional, used in production builds)
# SITE_URL=https://yourdomain.com
